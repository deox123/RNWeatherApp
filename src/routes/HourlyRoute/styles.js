import createStyles, {colors, dimensions, fonts, baseStyles} from '../../base.styles';

const HourlyStyles = createStyles({
    container: {
        ...baseStyles.container,
    },
});

export default HourlyStyles;
